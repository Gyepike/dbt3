version: 2

models:
  - name: stg_product_dbt
    columns:
      - name: order_id
        tests:
          - not_null
          - relationships:
              to: ref('stg_orders')
              field: id

      - name: id
        tests:
          - not_null
          - unique
