version: 2

sources:
  - name: public
    tables:
      - name: customer
      - name: orders

models:

  - name: stg_customers
    columns:
      - name: customer_id
        tests:
          - not_null
          - unique
  
  - name: stg_orders
    columns:
      - name: id
        tests:
          - not_null
          - unique

      - name: customer_id
        tests:
          - not_null