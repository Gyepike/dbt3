version: 2

models:
  - name: customer_orders
    columns:
      - name: customer_id
        tests:
          - not_null
          - unique
          - accepted_values:
              values: [1, 2, 3, 4, 5]
          - relationships:
              to: ref('stg_customers')
              field: customer_id
